// 在这个段落中你可以声明编译后的Jar文件信息
jar {
    baseName = 'taskmgr-admin'
}

dependencies {
    compile project(':common')
    compile("org.springframework.boot:spring-boot-starter-security")
    compile("io.jsonwebtoken:jjwt:${jjwtVersion}")
}
